module.exports = {
    root: true,
    env: {
        node: true,
    },
    extends: ['eslint:recommended'],
    // Rules and settings specific to your backend code
    rules: {
        // ... (backend-specific rules)
    },
};
